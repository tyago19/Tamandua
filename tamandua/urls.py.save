from django.conf.urls.defaults import *

# Uncomment the next two lines to enable the admin:
from django.contrib import admin
admin.autodiscover()

from objetos.models import Agencia, Funcionario, Cliente, Filiado

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'tamandua.views.home', name='home'),
    # url(r'^tamandua/', include('tamandua.foo.urls')),

    # Uncomment the admin/doc line below to enable admin documentation:
    # url(r'^admin/doc/', include('django.contrib.admindocs.urls')),

    # Uncomment the next line to enable the admin:
    url(r'^$', 'django.views.generic.date_based.archive_index',{'queryset': Agencia.objects.all()}),
    url(r'^$', 'django.views.generic.date_based.archive_index',{'queryset': Funcionario.objects.all()}),
    url(r'^$', 'django.views.generic.date_based.archive_index',{'queryset': Cliente.obj}),
    url(r'^$', 'django.views.generic.date_based.archive_index',{'queryset': Filiado.objects.all(),'date_field': 'publicacao'}),
    url(r'^admin/', include(admin.site.urls)),
)
